{
  "comment": "Generated by `npm run voice`. Empty clips means the film plays scored, with no narration — which is the state the shipped mp4 was rendered in. Committed empty so the composition always bundles and so a render never picks up a voice by accident.",
  "voice": null,
  "clips": []
}
